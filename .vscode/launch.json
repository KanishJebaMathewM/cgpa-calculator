{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Run Express Server",
            "program": "${workspaceFolder}/server.js",
            "env": {
                "PORT": "3000"
            }
        }
    ]
}
